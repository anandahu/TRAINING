// Online C compiler to run C program online
#include <stdio.h>

int main() {
    while(1){
        if(printf("%d",printf("%d")))
        break;
        else
        continue;
    }
    return 0;
}

OUTPUT:11



// Online C compiler to run C program online
#include <stdio.h>

int main() {
  int a;
  char *x;
  x=(char *)&a;
  a=512;
  x[0]=1;
  x[1]=2;
  printf("%d\n",a);
  
    return 0;
}

output:513



// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int a=1;
   int b=1;
   int c=a||--b;
   int d=a--&&--b;
   printf("a=%d,b=%d,c=%d,d=%d",a,b,c,d);
    return 0;
}

FOLIDS TRIANGLE

// Online C compiler to run C program online
#include <stdio.h>

int main() {
    int a=1;
    int b=5;
    for(int i=0;i<b;i++)
    {
        for(int j=0;j<=i;j++){
            printf("%d ",a++);
    
        }
                printf("\n");

        }
    
    
    return 0;
}
                                            04/06/2024


input=((A+b)-c*(d/e))+f
ans)initalize an empty stack
start scaning the infix operatons from the left to right 
for each charater in the infix exp
    if the charater is a number or variable output into the stack
    opening parathes (
